# homebrew-tap

> [!WARNING]
> **This tap is retired.** `journal` has moved to a personal tap:
>
> ```sh
> brew untap displacetech/tap        # optional cleanup
> brew install ericmann/tap/journal
> ```
>
> The `journal` cask here is frozen at **v2.4.0** and receives no further
> updates — see [ericmann/homebrew-tap](https://github.com/ericmann/homebrew-tap).

Homebrew formulae for Displace Technologies tools.
